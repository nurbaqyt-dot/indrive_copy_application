name: copy_app
description: "A complete inDrive-inspired Flutter/Firebase application."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  provider: ^6.1.2
  flutter_svg: ^2.0.17
  flutter_map: ^8.3.0
  latlong2: ^0.9.0
  google_fonts: ^8.1.0
  go_router: ^14.0.0
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  shared_preferences: ^2.3.0
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  url_launcher: ^6.2.0
  intl: ^0.19.0
  google_sign_in: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
